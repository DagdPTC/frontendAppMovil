// js/reservations.js

document.addEventListener('DOMContentLoaded', () => {
    const reservationsList = document.getElementById('reservations-list');
  
    // En el futuro, aquí se cargarían reservas reales desde la base de datos
    console.log("Reservas cargadas. Próximamente se conectarán a base de datos");
  });
  